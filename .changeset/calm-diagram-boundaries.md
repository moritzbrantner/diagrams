---
"@moritzbrantner/diagrams": minor
---

Add explicit server-safe `core` and client-side `react` entrypoints, serializable diagram view state, and a renderer-independent structured node/edge data contract.
