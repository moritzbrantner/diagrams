---
"@moritzbrantner/diagrams": minor
---

Add a renderer-independent Rust/WASM compute core for deterministic graph layout, orthogonal routing, bounds, and viewport visibility. Architecture diagrams now use the Rust auto-layout path when the runtime is installed while preserving explicitly authored coordinates and waypoints.
